import BaseAPI from "./BaseAPI";

class TagAPI extends BaseAPI {
  constructor() {
    super();
    this.controller += "/tags";
  }
  /**
   * Hàm login của AuthAPI
   * @param {*} payload
   * @returns
   */
}
export default new TagAPI();
